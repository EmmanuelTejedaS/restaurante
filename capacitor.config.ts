import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.makoslunch.makos',
  appName: 'makos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
